import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.dane.rapportstage',
  appName: 'Rapport de stage',
  webDir: 'dist',
}

export default config
